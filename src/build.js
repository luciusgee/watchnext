/* Release stamp, shown in Settings → About.
   "Still broken" and "hasn't updated yet" look identical in a bug report;
   this is what tells them apart. Lives in its own module so nothing has to
   import main.js to read it. */
export const BUILD = '20260808-1807';
